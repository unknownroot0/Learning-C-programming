Both printf and scanf use placeholders in the format string to specify the type of data being printed or read. Some common placeholders are:

   * %d for integers *
   * %f for floating-point numbers *
   * %c for characters *

Both printf and scanf are us
